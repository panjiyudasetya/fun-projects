function calculate() {
    // Define calculated calories object message
    var result = {
        "burned_cal" : "x101",
        "message" : "Test normal attribute"
    }
    return result;
}

calculate();